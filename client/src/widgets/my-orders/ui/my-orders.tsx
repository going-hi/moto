import { Typography } from '@/shared'

const { Title } = Typography

export const MyOrders = () => {
	return (
		<section>
			<Title variant='h5' className='-tracking-2per'>
				ЗАКАЗЫ
			</Title>
		</section>
	)
}
